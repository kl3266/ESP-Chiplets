
///////////////////////////////////////////////////////////////////////////////
//
// Copyright (c) 2021 Cadence Design Systems, Inc. All rights reserved worldwide.
//
// The code contained herein is the proprietary and confidential information 
// of Cadence or its licensors, and is supplied subject to a previously 
// executed license and maintenance agreement between Cadence and customer. 
// Permission is only granted for use with Cadence high-level synthesis tools and
// may not be used with other high-level synthesis tools. Permission is only 
// granted to distribute the code as indicated. Cadence grants permission for 
// customer to distribute a copy of this code to any partner to aid in designing 
// or verifying the customer's intellectual property, as long as such 
// distribution includes a restriction of no additional distributions from the 
// partner, unless the partner receives permission directly from Cadence.
//
// ALL CODE FURNISHED BY CADENCE IS PROVIDED "AS IS" WITHOUT WARRANTY OF ANY
// KIND, AND CADENCE SPECIFICALLY DISCLAIMS ANY WARRANTY OF NONINFRINGEMENT, 
// FITNESS FOR A PARTICULAR PURPOSE OR MERCHANTABILITY. CADENCE SHALL NOT BE 
// LIABLE FOR ANY COSTS OF PROCUREMENT OF SUBSTITUTES, LOSS OF PROFITS, 
// INTERRUPTION OF BUSINESS, OR FOR ANY OTHER SPECIAL, CONSEQUENTIAL OR 
// INCIDENTAL DAMAGES, HOWEVER CAUSED, WHETHER FOR BREACH OF WARRANTY, 
// CONTRACT, TORT, NEGLIGENCE, STRICT LIABILITY OR OTHERWISE.
//
////////////////////////////////////////////////////////////////////////////////


// Generated by bdw_memgen 24.02-s004
// Mon Dec 01 16:08:27 EST 2025

#if ! defined dummy_plm_CC
#define dummy_plm_CC 1
#if defined STRATUS  &&  ! defined CYN_DONT_SUPPRESS_MSGS
#pragma cyn_suppress_msgs NOTE
#endif	// STRATUS  &&  CYN_DONT_SUPPRESS_MSGS
#include "dummy_plm.h"
 
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write0(sc_uint<9> A0, sc_uint<64> D0, bool WE0, bool CE0, sc_uint<64> WEM0) {

      CYN_BIND(WE0,this->WE0);
      CYN_BIND(A0,this->A0);
      CYN_BIND(D0,this->D0);
      CYN_BIND(CE0,this->CE0);
      CYN_BIND(WEM0,this->WEM0);
      HLS_SET_PARAM( "access_port", 1 );
      mem[A0] = CYN_MASKED_READ( mem[A0], D0, WEM0 );
      return D0;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop0(bool WE0, bool CE0, sc_uint<64> WEM0) {
      CYN_BIND(WE0,this->WE0);
      CYN_BIND(CE0,this->CE0);
      CYN_BIND(WEM0,this->WEM0);
      HLS_SET_PARAM( "access_port", 1 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write1(sc_uint<9> A1, sc_uint<64> D1, bool WE1, bool CE1, sc_uint<64> WEM1) {

      CYN_BIND(WE1,this->WE1);
      CYN_BIND(A1,this->A1);
      CYN_BIND(D1,this->D1);
      CYN_BIND(CE1,this->CE1);
      CYN_BIND(WEM1,this->WEM1);
      HLS_SET_PARAM( "access_port", 2 );
      mem[A1] = CYN_MASKED_READ( mem[A1], D1, WEM1 );
      return D1;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop1(bool WE1, bool CE1, sc_uint<64> WEM1) {
      CYN_BIND(WE1,this->WE1);
      CYN_BIND(CE1,this->CE1);
      CYN_BIND(WEM1,this->WEM1);
      HLS_SET_PARAM( "access_port", 2 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write2(sc_uint<9> A2, sc_uint<64> D2, bool WE2, bool CE2, sc_uint<64> WEM2) {

      CYN_BIND(WE2,this->WE2);
      CYN_BIND(A2,this->A2);
      CYN_BIND(D2,this->D2);
      CYN_BIND(CE2,this->CE2);
      CYN_BIND(WEM2,this->WEM2);
      HLS_SET_PARAM( "access_port", 3 );
      mem[A2] = CYN_MASKED_READ( mem[A2], D2, WEM2 );
      return D2;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop2(bool WE2, bool CE2, sc_uint<64> WEM2) {
      CYN_BIND(WE2,this->WE2);
      CYN_BIND(CE2,this->CE2);
      CYN_BIND(WEM2,this->WEM2);
      HLS_SET_PARAM( "access_port", 3 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write3(sc_uint<9> A3, sc_uint<64> D3, bool WE3, bool CE3, sc_uint<64> WEM3) {

      CYN_BIND(WE3,this->WE3);
      CYN_BIND(A3,this->A3);
      CYN_BIND(D3,this->D3);
      CYN_BIND(CE3,this->CE3);
      CYN_BIND(WEM3,this->WEM3);
      HLS_SET_PARAM( "access_port", 4 );
      mem[A3] = CYN_MASKED_READ( mem[A3], D3, WEM3 );
      return D3;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop3(bool WE3, bool CE3, sc_uint<64> WEM3) {
      CYN_BIND(WE3,this->WE3);
      CYN_BIND(CE3,this->CE3);
      CYN_BIND(WEM3,this->WEM3);
      HLS_SET_PARAM( "access_port", 4 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write4(sc_uint<9> A4, sc_uint<64> D4, bool WE4, bool CE4, sc_uint<64> WEM4) {

      CYN_BIND(WE4,this->WE4);
      CYN_BIND(A4,this->A4);
      CYN_BIND(D4,this->D4);
      CYN_BIND(CE4,this->CE4);
      CYN_BIND(WEM4,this->WEM4);
      HLS_SET_PARAM( "access_port", 5 );
      mem[A4] = CYN_MASKED_READ( mem[A4], D4, WEM4 );
      return D4;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop4(bool WE4, bool CE4, sc_uint<64> WEM4) {
      CYN_BIND(WE4,this->WE4);
      CYN_BIND(CE4,this->CE4);
      CYN_BIND(WEM4,this->WEM4);
      HLS_SET_PARAM( "access_port", 5 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write5(sc_uint<9> A5, sc_uint<64> D5, bool WE5, bool CE5, sc_uint<64> WEM5) {

      CYN_BIND(WE5,this->WE5);
      CYN_BIND(A5,this->A5);
      CYN_BIND(D5,this->D5);
      CYN_BIND(CE5,this->CE5);
      CYN_BIND(WEM5,this->WEM5);
      HLS_SET_PARAM( "access_port", 6 );
      mem[A5] = CYN_MASKED_READ( mem[A5], D5, WEM5 );
      return D5;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop5(bool WE5, bool CE5, sc_uint<64> WEM5) {
      CYN_BIND(WE5,this->WE5);
      CYN_BIND(CE5,this->CE5);
      CYN_BIND(WEM5,this->WEM5);
      HLS_SET_PARAM( "access_port", 6 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write6(sc_uint<9> A6, sc_uint<64> D6, bool WE6, bool CE6, sc_uint<64> WEM6) {

      CYN_BIND(WE6,this->WE6);
      CYN_BIND(A6,this->A6);
      CYN_BIND(D6,this->D6);
      CYN_BIND(CE6,this->CE6);
      CYN_BIND(WEM6,this->WEM6);
      HLS_SET_PARAM( "access_port", 7 );
      mem[A6] = CYN_MASKED_READ( mem[A6], D6, WEM6 );
      return D6;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop6(bool WE6, bool CE6, sc_uint<64> WEM6) {
      CYN_BIND(WE6,this->WE6);
      CYN_BIND(CE6,this->CE6);
      CYN_BIND(WEM6,this->WEM6);
      HLS_SET_PARAM( "access_port", 7 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::write7(sc_uint<9> A7, sc_uint<64> D7, bool WE7, bool CE7, sc_uint<64> WEM7) {

      CYN_BIND(WE7,this->WE7);
      CYN_BIND(A7,this->A7);
      CYN_BIND(D7,this->D7);
      CYN_BIND(CE7,this->CE7);
      CYN_BIND(WEM7,this->WEM7);
      HLS_SET_PARAM( "access_port", 8 );
      mem[A7] = CYN_MASKED_READ( mem[A7], D7, WEM7 );
      return D7;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop7(bool WE7, bool CE7, sc_uint<64> WEM7) {
      CYN_BIND(WE7,this->WE7);
      CYN_BIND(CE7,this->CE7);
      CYN_BIND(WEM7,this->WEM7);
      HLS_SET_PARAM( "access_port", 8 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read8(sc_uint<9> A8, bool CE8) {
      sc_uint<64> Q8;
      CYN_BIND(A8,this->A8);
      CYN_BIND(Q8,this->Q8);
      CYN_BIND(CE8,this->CE8);
      HLS_SET_PARAM( "access_port", 9 );
      Q8 = mem[A8];
      return Q8;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop8(bool CE8) {
      CYN_BIND(CE8,this->CE8);
      HLS_SET_PARAM( "access_port", 9 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read9(sc_uint<9> A9, bool CE9) {
      sc_uint<64> Q9;
      CYN_BIND(A9,this->A9);
      CYN_BIND(Q9,this->Q9);
      CYN_BIND(CE9,this->CE9);
      HLS_SET_PARAM( "access_port", 10 );
      Q9 = mem[A9];
      return Q9;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop9(bool CE9) {
      CYN_BIND(CE9,this->CE9);
      HLS_SET_PARAM( "access_port", 10 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read10(sc_uint<9> A10, bool CE10) {
      sc_uint<64> Q10;
      CYN_BIND(A10,this->A10);
      CYN_BIND(Q10,this->Q10);
      CYN_BIND(CE10,this->CE10);
      HLS_SET_PARAM( "access_port", 11 );
      Q10 = mem[A10];
      return Q10;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop10(bool CE10) {
      CYN_BIND(CE10,this->CE10);
      HLS_SET_PARAM( "access_port", 11 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read11(sc_uint<9> A11, bool CE11) {
      sc_uint<64> Q11;
      CYN_BIND(A11,this->A11);
      CYN_BIND(Q11,this->Q11);
      CYN_BIND(CE11,this->CE11);
      HLS_SET_PARAM( "access_port", 12 );
      Q11 = mem[A11];
      return Q11;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop11(bool CE11) {
      CYN_BIND(CE11,this->CE11);
      HLS_SET_PARAM( "access_port", 12 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read12(sc_uint<9> A12, bool CE12) {
      sc_uint<64> Q12;
      CYN_BIND(A12,this->A12);
      CYN_BIND(Q12,this->Q12);
      CYN_BIND(CE12,this->CE12);
      HLS_SET_PARAM( "access_port", 13 );
      Q12 = mem[A12];
      return Q12;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop12(bool CE12) {
      CYN_BIND(CE12,this->CE12);
      HLS_SET_PARAM( "access_port", 13 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read13(sc_uint<9> A13, bool CE13) {
      sc_uint<64> Q13;
      CYN_BIND(A13,this->A13);
      CYN_BIND(Q13,this->Q13);
      CYN_BIND(CE13,this->CE13);
      HLS_SET_PARAM( "access_port", 14 );
      Q13 = mem[A13];
      return Q13;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop13(bool CE13) {
      CYN_BIND(CE13,this->CE13);
      HLS_SET_PARAM( "access_port", 14 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read14(sc_uint<9> A14, bool CE14) {
      sc_uint<64> Q14;
      CYN_BIND(A14,this->A14);
      CYN_BIND(Q14,this->Q14);
      CYN_BIND(CE14,this->CE14);
      HLS_SET_PARAM( "access_port", 15 );
      Q14 = mem[A14];
      return Q14;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop14(bool CE14) {
      CYN_BIND(CE14,this->CE14);
      HLS_SET_PARAM( "access_port", 15 );
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   sc_uint<64> dummy_plm::read15(sc_uint<9> A15, bool CE15) {
      sc_uint<64> Q15;
      CYN_BIND(A15,this->A15);
      CYN_BIND(Q15,this->Q15);
      CYN_BIND(CE15,this->CE15);
      HLS_SET_PARAM( "access_port", 16 );
      Q15 = mem[A15];
      return Q15;
   }

#endif	// STRATUS_HLS
#ifdef STRATUS_HLS
   void dummy_plm::nop15(bool CE15) {
      CYN_BIND(CE15,this->CE15);
      HLS_SET_PARAM( "access_port", 16 );
   }

#endif	// STRATUS_HLS
   void dummy_plm::thread0() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p1_tx_0.is_active() ) {
        m_p1_tx_stream->end_write_tx( m_p1_tx_0 );
    }
#endif
    if (CE0 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A0.read()) <= 511) {
#endif
        if(WE0.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A0.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A0.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A0.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM0.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D0.read().range(0,0).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(0,0) = D0.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D0.read().range(1,1).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(1,1) = D0.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D0.read().range(2,2).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(2,2) = D0.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D0.read().range(3,3).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(3,3) = D0.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D0.read().range(4,4).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(4,4) = D0.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D0.read().range(5,5).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(5,5) = D0.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D0.read().range(6,6).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(6,6) = D0.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D0.read().range(7,7).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(7,7) = D0.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D0.read().range(8,8).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(8,8) = D0.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D0.read().range(9,9).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(9,9) = D0.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D0.read().range(10,10).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(10,10) = D0.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D0.read().range(11,11).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(11,11) = D0.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D0.read().range(12,12).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(12,12) = D0.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D0.read().range(13,13).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(13,13) = D0.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D0.read().range(14,14).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(14,14) = D0.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D0.read().range(15,15).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(15,15) = D0.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D0.read().range(16,16).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(16,16) = D0.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D0.read().range(17,17).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(17,17) = D0.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D0.read().range(18,18).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(18,18) = D0.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D0.read().range(19,19).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(19,19) = D0.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D0.read().range(20,20).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(20,20) = D0.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D0.read().range(21,21).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(21,21) = D0.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D0.read().range(22,22).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(22,22) = D0.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D0.read().range(23,23).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(23,23) = D0.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D0.read().range(24,24).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(24,24) = D0.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D0.read().range(25,25).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(25,25) = D0.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D0.read().range(26,26).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(26,26) = D0.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D0.read().range(27,27).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(27,27) = D0.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D0.read().range(28,28).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(28,28) = D0.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D0.read().range(29,29).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(29,29) = D0.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D0.read().range(30,30).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(30,30) = D0.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D0.read().range(31,31).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(31,31) = D0.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D0.read().range(32,32).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(32,32) = D0.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D0.read().range(33,33).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(33,33) = D0.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D0.read().range(34,34).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(34,34) = D0.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D0.read().range(35,35).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(35,35) = D0.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D0.read().range(36,36).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(36,36) = D0.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D0.read().range(37,37).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(37,37) = D0.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D0.read().range(38,38).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(38,38) = D0.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D0.read().range(39,39).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(39,39) = D0.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D0.read().range(40,40).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(40,40) = D0.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D0.read().range(41,41).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(41,41) = D0.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D0.read().range(42,42).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(42,42) = D0.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D0.read().range(43,43).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(43,43) = D0.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D0.read().range(44,44).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(44,44) = D0.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D0.read().range(45,45).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(45,45) = D0.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D0.read().range(46,46).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(46,46) = D0.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D0.read().range(47,47).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(47,47) = D0.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D0.read().range(48,48).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(48,48) = D0.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D0.read().range(49,49).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(49,49) = D0.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D0.read().range(50,50).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(50,50) = D0.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D0.read().range(51,51).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(51,51) = D0.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D0.read().range(52,52).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(52,52) = D0.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D0.read().range(53,53).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(53,53) = D0.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D0.read().range(54,54).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(54,54) = D0.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D0.read().range(55,55).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(55,55) = D0.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D0.read().range(56,56).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(56,56) = D0.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D0.read().range(57,57).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(57,57) = D0.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D0.read().range(58,58).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(58,58) = D0.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D0.read().range(59,59).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(59,59) = D0.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D0.read().range(60,60).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(60,60) = D0.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D0.read().range(61,61).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(61,61) = D0.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D0.read().range(62,62).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(62,62) = D0.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
           if ( WEM0.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p1_tx_0 = m_p1_tx_stream->begin_write_tx( A0.read(), D0.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D0.read().range(63,63).to_string(SC_HEX).c_str(), (int)A0.read().to_uint());
#endif
        mem[A0.read().to_uint()].range(63,63) = D0.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A0.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread1() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p2_tx_0.is_active() ) {
        m_p2_tx_stream->end_write_tx( m_p2_tx_0 );
    }
#endif
    if (CE1 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A1.read()) <= 511) {
#endif
        if(WE1.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A1.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A1.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A1.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM1.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D1.read().range(0,0).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(0,0) = D1.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D1.read().range(1,1).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(1,1) = D1.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D1.read().range(2,2).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(2,2) = D1.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D1.read().range(3,3).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(3,3) = D1.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D1.read().range(4,4).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(4,4) = D1.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D1.read().range(5,5).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(5,5) = D1.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D1.read().range(6,6).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(6,6) = D1.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D1.read().range(7,7).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(7,7) = D1.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D1.read().range(8,8).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(8,8) = D1.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D1.read().range(9,9).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(9,9) = D1.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D1.read().range(10,10).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(10,10) = D1.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D1.read().range(11,11).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(11,11) = D1.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D1.read().range(12,12).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(12,12) = D1.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D1.read().range(13,13).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(13,13) = D1.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D1.read().range(14,14).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(14,14) = D1.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D1.read().range(15,15).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(15,15) = D1.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D1.read().range(16,16).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(16,16) = D1.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D1.read().range(17,17).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(17,17) = D1.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D1.read().range(18,18).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(18,18) = D1.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D1.read().range(19,19).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(19,19) = D1.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D1.read().range(20,20).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(20,20) = D1.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D1.read().range(21,21).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(21,21) = D1.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D1.read().range(22,22).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(22,22) = D1.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D1.read().range(23,23).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(23,23) = D1.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D1.read().range(24,24).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(24,24) = D1.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D1.read().range(25,25).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(25,25) = D1.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D1.read().range(26,26).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(26,26) = D1.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D1.read().range(27,27).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(27,27) = D1.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D1.read().range(28,28).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(28,28) = D1.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D1.read().range(29,29).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(29,29) = D1.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D1.read().range(30,30).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(30,30) = D1.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D1.read().range(31,31).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(31,31) = D1.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D1.read().range(32,32).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(32,32) = D1.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D1.read().range(33,33).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(33,33) = D1.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D1.read().range(34,34).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(34,34) = D1.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D1.read().range(35,35).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(35,35) = D1.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D1.read().range(36,36).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(36,36) = D1.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D1.read().range(37,37).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(37,37) = D1.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D1.read().range(38,38).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(38,38) = D1.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D1.read().range(39,39).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(39,39) = D1.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D1.read().range(40,40).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(40,40) = D1.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D1.read().range(41,41).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(41,41) = D1.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D1.read().range(42,42).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(42,42) = D1.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D1.read().range(43,43).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(43,43) = D1.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D1.read().range(44,44).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(44,44) = D1.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D1.read().range(45,45).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(45,45) = D1.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D1.read().range(46,46).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(46,46) = D1.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D1.read().range(47,47).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(47,47) = D1.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D1.read().range(48,48).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(48,48) = D1.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D1.read().range(49,49).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(49,49) = D1.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D1.read().range(50,50).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(50,50) = D1.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D1.read().range(51,51).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(51,51) = D1.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D1.read().range(52,52).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(52,52) = D1.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D1.read().range(53,53).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(53,53) = D1.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D1.read().range(54,54).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(54,54) = D1.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D1.read().range(55,55).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(55,55) = D1.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D1.read().range(56,56).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(56,56) = D1.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D1.read().range(57,57).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(57,57) = D1.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D1.read().range(58,58).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(58,58) = D1.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D1.read().range(59,59).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(59,59) = D1.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D1.read().range(60,60).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(60,60) = D1.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D1.read().range(61,61).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(61,61) = D1.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D1.read().range(62,62).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(62,62) = D1.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
           if ( WEM1.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p2_tx_0 = m_p2_tx_stream->begin_write_tx( A1.read(), D1.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D1.read().range(63,63).to_string(SC_HEX).c_str(), (int)A1.read().to_uint());
#endif
        mem[A1.read().to_uint()].range(63,63) = D1.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A1.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread2() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p3_tx_0.is_active() ) {
        m_p3_tx_stream->end_write_tx( m_p3_tx_0 );
    }
#endif
    if (CE2 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A2.read()) <= 511) {
#endif
        if(WE2.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A2.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A2.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A2.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM2.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D2.read().range(0,0).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(0,0) = D2.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D2.read().range(1,1).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(1,1) = D2.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D2.read().range(2,2).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(2,2) = D2.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D2.read().range(3,3).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(3,3) = D2.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D2.read().range(4,4).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(4,4) = D2.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D2.read().range(5,5).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(5,5) = D2.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D2.read().range(6,6).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(6,6) = D2.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D2.read().range(7,7).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(7,7) = D2.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D2.read().range(8,8).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(8,8) = D2.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D2.read().range(9,9).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(9,9) = D2.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D2.read().range(10,10).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(10,10) = D2.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D2.read().range(11,11).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(11,11) = D2.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D2.read().range(12,12).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(12,12) = D2.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D2.read().range(13,13).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(13,13) = D2.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D2.read().range(14,14).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(14,14) = D2.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D2.read().range(15,15).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(15,15) = D2.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D2.read().range(16,16).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(16,16) = D2.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D2.read().range(17,17).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(17,17) = D2.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D2.read().range(18,18).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(18,18) = D2.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D2.read().range(19,19).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(19,19) = D2.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D2.read().range(20,20).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(20,20) = D2.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D2.read().range(21,21).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(21,21) = D2.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D2.read().range(22,22).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(22,22) = D2.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D2.read().range(23,23).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(23,23) = D2.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D2.read().range(24,24).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(24,24) = D2.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D2.read().range(25,25).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(25,25) = D2.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D2.read().range(26,26).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(26,26) = D2.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D2.read().range(27,27).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(27,27) = D2.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D2.read().range(28,28).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(28,28) = D2.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D2.read().range(29,29).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(29,29) = D2.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D2.read().range(30,30).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(30,30) = D2.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D2.read().range(31,31).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(31,31) = D2.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D2.read().range(32,32).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(32,32) = D2.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D2.read().range(33,33).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(33,33) = D2.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D2.read().range(34,34).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(34,34) = D2.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D2.read().range(35,35).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(35,35) = D2.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D2.read().range(36,36).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(36,36) = D2.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D2.read().range(37,37).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(37,37) = D2.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D2.read().range(38,38).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(38,38) = D2.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D2.read().range(39,39).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(39,39) = D2.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D2.read().range(40,40).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(40,40) = D2.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D2.read().range(41,41).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(41,41) = D2.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D2.read().range(42,42).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(42,42) = D2.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D2.read().range(43,43).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(43,43) = D2.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D2.read().range(44,44).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(44,44) = D2.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D2.read().range(45,45).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(45,45) = D2.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D2.read().range(46,46).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(46,46) = D2.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D2.read().range(47,47).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(47,47) = D2.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D2.read().range(48,48).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(48,48) = D2.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D2.read().range(49,49).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(49,49) = D2.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D2.read().range(50,50).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(50,50) = D2.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D2.read().range(51,51).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(51,51) = D2.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D2.read().range(52,52).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(52,52) = D2.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D2.read().range(53,53).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(53,53) = D2.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D2.read().range(54,54).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(54,54) = D2.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D2.read().range(55,55).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(55,55) = D2.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D2.read().range(56,56).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(56,56) = D2.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D2.read().range(57,57).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(57,57) = D2.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D2.read().range(58,58).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(58,58) = D2.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D2.read().range(59,59).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(59,59) = D2.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D2.read().range(60,60).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(60,60) = D2.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D2.read().range(61,61).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(61,61) = D2.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D2.read().range(62,62).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(62,62) = D2.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
           if ( WEM2.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p3_tx_0 = m_p3_tx_stream->begin_write_tx( A2.read(), D2.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D2.read().range(63,63).to_string(SC_HEX).c_str(), (int)A2.read().to_uint());
#endif
        mem[A2.read().to_uint()].range(63,63) = D2.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A2.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread3() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p4_tx_0.is_active() ) {
        m_p4_tx_stream->end_write_tx( m_p4_tx_0 );
    }
#endif
    if (CE3 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A3.read()) <= 511) {
#endif
        if(WE3.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A3.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A3.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A3.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM3.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D3.read().range(0,0).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(0,0) = D3.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D3.read().range(1,1).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(1,1) = D3.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D3.read().range(2,2).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(2,2) = D3.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D3.read().range(3,3).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(3,3) = D3.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D3.read().range(4,4).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(4,4) = D3.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D3.read().range(5,5).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(5,5) = D3.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D3.read().range(6,6).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(6,6) = D3.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D3.read().range(7,7).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(7,7) = D3.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D3.read().range(8,8).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(8,8) = D3.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D3.read().range(9,9).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(9,9) = D3.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D3.read().range(10,10).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(10,10) = D3.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D3.read().range(11,11).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(11,11) = D3.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D3.read().range(12,12).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(12,12) = D3.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D3.read().range(13,13).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(13,13) = D3.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D3.read().range(14,14).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(14,14) = D3.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D3.read().range(15,15).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(15,15) = D3.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D3.read().range(16,16).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(16,16) = D3.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D3.read().range(17,17).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(17,17) = D3.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D3.read().range(18,18).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(18,18) = D3.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D3.read().range(19,19).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(19,19) = D3.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D3.read().range(20,20).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(20,20) = D3.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D3.read().range(21,21).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(21,21) = D3.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D3.read().range(22,22).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(22,22) = D3.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D3.read().range(23,23).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(23,23) = D3.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D3.read().range(24,24).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(24,24) = D3.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D3.read().range(25,25).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(25,25) = D3.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D3.read().range(26,26).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(26,26) = D3.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D3.read().range(27,27).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(27,27) = D3.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D3.read().range(28,28).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(28,28) = D3.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D3.read().range(29,29).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(29,29) = D3.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D3.read().range(30,30).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(30,30) = D3.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D3.read().range(31,31).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(31,31) = D3.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D3.read().range(32,32).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(32,32) = D3.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D3.read().range(33,33).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(33,33) = D3.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D3.read().range(34,34).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(34,34) = D3.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D3.read().range(35,35).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(35,35) = D3.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D3.read().range(36,36).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(36,36) = D3.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D3.read().range(37,37).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(37,37) = D3.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D3.read().range(38,38).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(38,38) = D3.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D3.read().range(39,39).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(39,39) = D3.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D3.read().range(40,40).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(40,40) = D3.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D3.read().range(41,41).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(41,41) = D3.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D3.read().range(42,42).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(42,42) = D3.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D3.read().range(43,43).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(43,43) = D3.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D3.read().range(44,44).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(44,44) = D3.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D3.read().range(45,45).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(45,45) = D3.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D3.read().range(46,46).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(46,46) = D3.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D3.read().range(47,47).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(47,47) = D3.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D3.read().range(48,48).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(48,48) = D3.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D3.read().range(49,49).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(49,49) = D3.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D3.read().range(50,50).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(50,50) = D3.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D3.read().range(51,51).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(51,51) = D3.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D3.read().range(52,52).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(52,52) = D3.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D3.read().range(53,53).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(53,53) = D3.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D3.read().range(54,54).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(54,54) = D3.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D3.read().range(55,55).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(55,55) = D3.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D3.read().range(56,56).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(56,56) = D3.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D3.read().range(57,57).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(57,57) = D3.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D3.read().range(58,58).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(58,58) = D3.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D3.read().range(59,59).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(59,59) = D3.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D3.read().range(60,60).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(60,60) = D3.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D3.read().range(61,61).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(61,61) = D3.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D3.read().range(62,62).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(62,62) = D3.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
           if ( WEM3.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p4_tx_0 = m_p4_tx_stream->begin_write_tx( A3.read(), D3.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D3.read().range(63,63).to_string(SC_HEX).c_str(), (int)A3.read().to_uint());
#endif
        mem[A3.read().to_uint()].range(63,63) = D3.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A3.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread4() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p5_tx_0.is_active() ) {
        m_p5_tx_stream->end_write_tx( m_p5_tx_0 );
    }
#endif
    if (CE4 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A4.read()) <= 511) {
#endif
        if(WE4.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A4.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A4.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A4.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM4.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D4.read().range(0,0).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(0,0) = D4.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D4.read().range(1,1).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(1,1) = D4.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D4.read().range(2,2).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(2,2) = D4.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D4.read().range(3,3).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(3,3) = D4.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D4.read().range(4,4).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(4,4) = D4.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D4.read().range(5,5).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(5,5) = D4.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D4.read().range(6,6).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(6,6) = D4.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D4.read().range(7,7).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(7,7) = D4.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D4.read().range(8,8).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(8,8) = D4.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D4.read().range(9,9).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(9,9) = D4.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D4.read().range(10,10).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(10,10) = D4.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D4.read().range(11,11).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(11,11) = D4.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D4.read().range(12,12).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(12,12) = D4.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D4.read().range(13,13).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(13,13) = D4.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D4.read().range(14,14).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(14,14) = D4.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D4.read().range(15,15).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(15,15) = D4.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D4.read().range(16,16).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(16,16) = D4.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D4.read().range(17,17).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(17,17) = D4.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D4.read().range(18,18).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(18,18) = D4.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D4.read().range(19,19).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(19,19) = D4.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D4.read().range(20,20).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(20,20) = D4.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D4.read().range(21,21).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(21,21) = D4.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D4.read().range(22,22).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(22,22) = D4.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D4.read().range(23,23).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(23,23) = D4.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D4.read().range(24,24).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(24,24) = D4.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D4.read().range(25,25).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(25,25) = D4.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D4.read().range(26,26).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(26,26) = D4.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D4.read().range(27,27).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(27,27) = D4.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D4.read().range(28,28).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(28,28) = D4.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D4.read().range(29,29).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(29,29) = D4.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D4.read().range(30,30).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(30,30) = D4.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D4.read().range(31,31).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(31,31) = D4.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D4.read().range(32,32).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(32,32) = D4.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D4.read().range(33,33).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(33,33) = D4.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D4.read().range(34,34).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(34,34) = D4.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D4.read().range(35,35).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(35,35) = D4.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D4.read().range(36,36).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(36,36) = D4.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D4.read().range(37,37).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(37,37) = D4.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D4.read().range(38,38).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(38,38) = D4.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D4.read().range(39,39).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(39,39) = D4.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D4.read().range(40,40).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(40,40) = D4.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D4.read().range(41,41).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(41,41) = D4.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D4.read().range(42,42).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(42,42) = D4.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D4.read().range(43,43).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(43,43) = D4.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D4.read().range(44,44).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(44,44) = D4.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D4.read().range(45,45).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(45,45) = D4.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D4.read().range(46,46).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(46,46) = D4.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D4.read().range(47,47).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(47,47) = D4.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D4.read().range(48,48).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(48,48) = D4.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D4.read().range(49,49).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(49,49) = D4.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D4.read().range(50,50).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(50,50) = D4.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D4.read().range(51,51).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(51,51) = D4.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D4.read().range(52,52).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(52,52) = D4.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D4.read().range(53,53).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(53,53) = D4.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D4.read().range(54,54).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(54,54) = D4.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D4.read().range(55,55).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(55,55) = D4.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D4.read().range(56,56).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(56,56) = D4.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D4.read().range(57,57).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(57,57) = D4.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D4.read().range(58,58).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(58,58) = D4.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D4.read().range(59,59).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(59,59) = D4.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D4.read().range(60,60).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(60,60) = D4.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D4.read().range(61,61).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(61,61) = D4.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D4.read().range(62,62).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(62,62) = D4.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
           if ( WEM4.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p5_tx_0 = m_p5_tx_stream->begin_write_tx( A4.read(), D4.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D4.read().range(63,63).to_string(SC_HEX).c_str(), (int)A4.read().to_uint());
#endif
        mem[A4.read().to_uint()].range(63,63) = D4.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A4.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread5() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p6_tx_0.is_active() ) {
        m_p6_tx_stream->end_write_tx( m_p6_tx_0 );
    }
#endif
    if (CE5 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A5.read()) <= 511) {
#endif
        if(WE5.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A5.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A5.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A5.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM5.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D5.read().range(0,0).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(0,0) = D5.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D5.read().range(1,1).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(1,1) = D5.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D5.read().range(2,2).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(2,2) = D5.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D5.read().range(3,3).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(3,3) = D5.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D5.read().range(4,4).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(4,4) = D5.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D5.read().range(5,5).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(5,5) = D5.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D5.read().range(6,6).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(6,6) = D5.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D5.read().range(7,7).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(7,7) = D5.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D5.read().range(8,8).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(8,8) = D5.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D5.read().range(9,9).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(9,9) = D5.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D5.read().range(10,10).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(10,10) = D5.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D5.read().range(11,11).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(11,11) = D5.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D5.read().range(12,12).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(12,12) = D5.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D5.read().range(13,13).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(13,13) = D5.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D5.read().range(14,14).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(14,14) = D5.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D5.read().range(15,15).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(15,15) = D5.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D5.read().range(16,16).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(16,16) = D5.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D5.read().range(17,17).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(17,17) = D5.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D5.read().range(18,18).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(18,18) = D5.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D5.read().range(19,19).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(19,19) = D5.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D5.read().range(20,20).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(20,20) = D5.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D5.read().range(21,21).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(21,21) = D5.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D5.read().range(22,22).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(22,22) = D5.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D5.read().range(23,23).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(23,23) = D5.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D5.read().range(24,24).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(24,24) = D5.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D5.read().range(25,25).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(25,25) = D5.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D5.read().range(26,26).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(26,26) = D5.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D5.read().range(27,27).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(27,27) = D5.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D5.read().range(28,28).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(28,28) = D5.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D5.read().range(29,29).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(29,29) = D5.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D5.read().range(30,30).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(30,30) = D5.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D5.read().range(31,31).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(31,31) = D5.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D5.read().range(32,32).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(32,32) = D5.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D5.read().range(33,33).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(33,33) = D5.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D5.read().range(34,34).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(34,34) = D5.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D5.read().range(35,35).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(35,35) = D5.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D5.read().range(36,36).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(36,36) = D5.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D5.read().range(37,37).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(37,37) = D5.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D5.read().range(38,38).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(38,38) = D5.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D5.read().range(39,39).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(39,39) = D5.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D5.read().range(40,40).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(40,40) = D5.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D5.read().range(41,41).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(41,41) = D5.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D5.read().range(42,42).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(42,42) = D5.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D5.read().range(43,43).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(43,43) = D5.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D5.read().range(44,44).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(44,44) = D5.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D5.read().range(45,45).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(45,45) = D5.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D5.read().range(46,46).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(46,46) = D5.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D5.read().range(47,47).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(47,47) = D5.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D5.read().range(48,48).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(48,48) = D5.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D5.read().range(49,49).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(49,49) = D5.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D5.read().range(50,50).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(50,50) = D5.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D5.read().range(51,51).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(51,51) = D5.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D5.read().range(52,52).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(52,52) = D5.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D5.read().range(53,53).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(53,53) = D5.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D5.read().range(54,54).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(54,54) = D5.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D5.read().range(55,55).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(55,55) = D5.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D5.read().range(56,56).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(56,56) = D5.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D5.read().range(57,57).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(57,57) = D5.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D5.read().range(58,58).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(58,58) = D5.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D5.read().range(59,59).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(59,59) = D5.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D5.read().range(60,60).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(60,60) = D5.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D5.read().range(61,61).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(61,61) = D5.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D5.read().range(62,62).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(62,62) = D5.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
           if ( WEM5.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p6_tx_0 = m_p6_tx_stream->begin_write_tx( A5.read(), D5.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D5.read().range(63,63).to_string(SC_HEX).c_str(), (int)A5.read().to_uint());
#endif
        mem[A5.read().to_uint()].range(63,63) = D5.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A5.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread6() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p7_tx_0.is_active() ) {
        m_p7_tx_stream->end_write_tx( m_p7_tx_0 );
    }
#endif
    if (CE6 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A6.read()) <= 511) {
#endif
        if(WE6.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A6.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A6.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A6.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM6.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D6.read().range(0,0).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(0,0) = D6.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D6.read().range(1,1).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(1,1) = D6.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D6.read().range(2,2).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(2,2) = D6.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D6.read().range(3,3).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(3,3) = D6.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D6.read().range(4,4).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(4,4) = D6.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D6.read().range(5,5).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(5,5) = D6.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D6.read().range(6,6).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(6,6) = D6.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D6.read().range(7,7).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(7,7) = D6.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D6.read().range(8,8).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(8,8) = D6.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D6.read().range(9,9).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(9,9) = D6.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D6.read().range(10,10).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(10,10) = D6.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D6.read().range(11,11).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(11,11) = D6.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D6.read().range(12,12).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(12,12) = D6.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D6.read().range(13,13).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(13,13) = D6.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D6.read().range(14,14).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(14,14) = D6.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D6.read().range(15,15).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(15,15) = D6.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D6.read().range(16,16).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(16,16) = D6.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D6.read().range(17,17).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(17,17) = D6.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D6.read().range(18,18).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(18,18) = D6.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D6.read().range(19,19).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(19,19) = D6.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D6.read().range(20,20).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(20,20) = D6.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D6.read().range(21,21).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(21,21) = D6.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D6.read().range(22,22).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(22,22) = D6.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D6.read().range(23,23).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(23,23) = D6.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D6.read().range(24,24).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(24,24) = D6.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D6.read().range(25,25).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(25,25) = D6.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D6.read().range(26,26).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(26,26) = D6.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D6.read().range(27,27).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(27,27) = D6.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D6.read().range(28,28).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(28,28) = D6.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D6.read().range(29,29).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(29,29) = D6.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D6.read().range(30,30).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(30,30) = D6.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D6.read().range(31,31).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(31,31) = D6.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D6.read().range(32,32).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(32,32) = D6.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D6.read().range(33,33).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(33,33) = D6.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D6.read().range(34,34).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(34,34) = D6.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D6.read().range(35,35).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(35,35) = D6.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D6.read().range(36,36).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(36,36) = D6.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D6.read().range(37,37).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(37,37) = D6.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D6.read().range(38,38).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(38,38) = D6.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D6.read().range(39,39).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(39,39) = D6.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D6.read().range(40,40).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(40,40) = D6.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D6.read().range(41,41).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(41,41) = D6.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D6.read().range(42,42).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(42,42) = D6.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D6.read().range(43,43).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(43,43) = D6.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D6.read().range(44,44).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(44,44) = D6.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D6.read().range(45,45).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(45,45) = D6.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D6.read().range(46,46).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(46,46) = D6.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D6.read().range(47,47).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(47,47) = D6.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D6.read().range(48,48).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(48,48) = D6.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D6.read().range(49,49).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(49,49) = D6.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D6.read().range(50,50).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(50,50) = D6.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D6.read().range(51,51).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(51,51) = D6.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D6.read().range(52,52).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(52,52) = D6.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D6.read().range(53,53).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(53,53) = D6.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D6.read().range(54,54).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(54,54) = D6.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D6.read().range(55,55).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(55,55) = D6.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D6.read().range(56,56).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(56,56) = D6.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D6.read().range(57,57).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(57,57) = D6.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D6.read().range(58,58).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(58,58) = D6.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D6.read().range(59,59).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(59,59) = D6.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D6.read().range(60,60).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(60,60) = D6.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D6.read().range(61,61).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(61,61) = D6.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D6.read().range(62,62).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(62,62) = D6.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
           if ( WEM6.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p7_tx_0 = m_p7_tx_stream->begin_write_tx( A6.read(), D6.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D6.read().range(63,63).to_string(SC_HEX).c_str(), (int)A6.read().to_uint());
#endif
        mem[A6.read().to_uint()].range(63,63) = D6.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A6.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread7() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p8_tx_0.is_active() ) {
        m_p8_tx_stream->end_write_tx( m_p8_tx_0 );
    }
#endif
    if (CE7 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A7.read()) <= 511) {
#endif
        if(WE7.read() == 1) {

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_read_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_read_addr == A7.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A7.read());
          }
          m_last_write_time = sc_time_stamp().to_double();
          m_last_write_addr = A7.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif
           if ( WEM7.read()[0] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".0", sc_time_stamp().to_string().c_str(), D7.read().range(0,0).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(0,0) = D7.read().range(0,0);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[1] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".1", sc_time_stamp().to_string().c_str(), D7.read().range(1,1).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(1,1) = D7.read().range(1,1);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[2] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".2", sc_time_stamp().to_string().c_str(), D7.read().range(2,2).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(2,2) = D7.read().range(2,2);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[3] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".3", sc_time_stamp().to_string().c_str(), D7.read().range(3,3).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(3,3) = D7.read().range(3,3);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[4] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".4", sc_time_stamp().to_string().c_str(), D7.read().range(4,4).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(4,4) = D7.read().range(4,4);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[5] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".5", sc_time_stamp().to_string().c_str(), D7.read().range(5,5).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(5,5) = D7.read().range(5,5);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[6] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".6", sc_time_stamp().to_string().c_str(), D7.read().range(6,6).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(6,6) = D7.read().range(6,6);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[7] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".7", sc_time_stamp().to_string().c_str(), D7.read().range(7,7).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(7,7) = D7.read().range(7,7);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[8] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".8", sc_time_stamp().to_string().c_str(), D7.read().range(8,8).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(8,8) = D7.read().range(8,8);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[9] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".9", sc_time_stamp().to_string().c_str(), D7.read().range(9,9).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(9,9) = D7.read().range(9,9);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[10] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".10", sc_time_stamp().to_string().c_str(), D7.read().range(10,10).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(10,10) = D7.read().range(10,10);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[11] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".11", sc_time_stamp().to_string().c_str(), D7.read().range(11,11).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(11,11) = D7.read().range(11,11);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[12] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".12", sc_time_stamp().to_string().c_str(), D7.read().range(12,12).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(12,12) = D7.read().range(12,12);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[13] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".13", sc_time_stamp().to_string().c_str(), D7.read().range(13,13).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(13,13) = D7.read().range(13,13);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[14] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".14", sc_time_stamp().to_string().c_str(), D7.read().range(14,14).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(14,14) = D7.read().range(14,14);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[15] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".15", sc_time_stamp().to_string().c_str(), D7.read().range(15,15).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(15,15) = D7.read().range(15,15);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[16] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".16", sc_time_stamp().to_string().c_str(), D7.read().range(16,16).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(16,16) = D7.read().range(16,16);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[17] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".17", sc_time_stamp().to_string().c_str(), D7.read().range(17,17).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(17,17) = D7.read().range(17,17);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[18] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".18", sc_time_stamp().to_string().c_str(), D7.read().range(18,18).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(18,18) = D7.read().range(18,18);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[19] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".19", sc_time_stamp().to_string().c_str(), D7.read().range(19,19).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(19,19) = D7.read().range(19,19);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[20] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".20", sc_time_stamp().to_string().c_str(), D7.read().range(20,20).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(20,20) = D7.read().range(20,20);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[21] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".21", sc_time_stamp().to_string().c_str(), D7.read().range(21,21).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(21,21) = D7.read().range(21,21);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[22] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".22", sc_time_stamp().to_string().c_str(), D7.read().range(22,22).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(22,22) = D7.read().range(22,22);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[23] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".23", sc_time_stamp().to_string().c_str(), D7.read().range(23,23).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(23,23) = D7.read().range(23,23);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[24] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".24", sc_time_stamp().to_string().c_str(), D7.read().range(24,24).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(24,24) = D7.read().range(24,24);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[25] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".25", sc_time_stamp().to_string().c_str(), D7.read().range(25,25).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(25,25) = D7.read().range(25,25);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[26] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".26", sc_time_stamp().to_string().c_str(), D7.read().range(26,26).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(26,26) = D7.read().range(26,26);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[27] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".27", sc_time_stamp().to_string().c_str(), D7.read().range(27,27).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(27,27) = D7.read().range(27,27);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[28] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".28", sc_time_stamp().to_string().c_str(), D7.read().range(28,28).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(28,28) = D7.read().range(28,28);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[29] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".29", sc_time_stamp().to_string().c_str(), D7.read().range(29,29).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(29,29) = D7.read().range(29,29);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[30] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".30", sc_time_stamp().to_string().c_str(), D7.read().range(30,30).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(30,30) = D7.read().range(30,30);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[31] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".31", sc_time_stamp().to_string().c_str(), D7.read().range(31,31).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(31,31) = D7.read().range(31,31);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[32] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".32", sc_time_stamp().to_string().c_str(), D7.read().range(32,32).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(32,32) = D7.read().range(32,32);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[33] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".33", sc_time_stamp().to_string().c_str(), D7.read().range(33,33).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(33,33) = D7.read().range(33,33);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[34] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".34", sc_time_stamp().to_string().c_str(), D7.read().range(34,34).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(34,34) = D7.read().range(34,34);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[35] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".35", sc_time_stamp().to_string().c_str(), D7.read().range(35,35).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(35,35) = D7.read().range(35,35);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[36] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".36", sc_time_stamp().to_string().c_str(), D7.read().range(36,36).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(36,36) = D7.read().range(36,36);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[37] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".37", sc_time_stamp().to_string().c_str(), D7.read().range(37,37).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(37,37) = D7.read().range(37,37);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[38] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".38", sc_time_stamp().to_string().c_str(), D7.read().range(38,38).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(38,38) = D7.read().range(38,38);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[39] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".39", sc_time_stamp().to_string().c_str(), D7.read().range(39,39).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(39,39) = D7.read().range(39,39);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[40] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".40", sc_time_stamp().to_string().c_str(), D7.read().range(40,40).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(40,40) = D7.read().range(40,40);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[41] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".41", sc_time_stamp().to_string().c_str(), D7.read().range(41,41).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(41,41) = D7.read().range(41,41);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[42] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".42", sc_time_stamp().to_string().c_str(), D7.read().range(42,42).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(42,42) = D7.read().range(42,42);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[43] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".43", sc_time_stamp().to_string().c_str(), D7.read().range(43,43).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(43,43) = D7.read().range(43,43);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[44] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".44", sc_time_stamp().to_string().c_str(), D7.read().range(44,44).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(44,44) = D7.read().range(44,44);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[45] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".45", sc_time_stamp().to_string().c_str(), D7.read().range(45,45).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(45,45) = D7.read().range(45,45);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[46] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".46", sc_time_stamp().to_string().c_str(), D7.read().range(46,46).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(46,46) = D7.read().range(46,46);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[47] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".47", sc_time_stamp().to_string().c_str(), D7.read().range(47,47).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(47,47) = D7.read().range(47,47);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[48] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".48", sc_time_stamp().to_string().c_str(), D7.read().range(48,48).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(48,48) = D7.read().range(48,48);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[49] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".49", sc_time_stamp().to_string().c_str(), D7.read().range(49,49).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(49,49) = D7.read().range(49,49);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[50] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".50", sc_time_stamp().to_string().c_str(), D7.read().range(50,50).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(50,50) = D7.read().range(50,50);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[51] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".51", sc_time_stamp().to_string().c_str(), D7.read().range(51,51).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(51,51) = D7.read().range(51,51);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[52] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".52", sc_time_stamp().to_string().c_str(), D7.read().range(52,52).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(52,52) = D7.read().range(52,52);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[53] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".53", sc_time_stamp().to_string().c_str(), D7.read().range(53,53).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(53,53) = D7.read().range(53,53);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[54] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".54", sc_time_stamp().to_string().c_str(), D7.read().range(54,54).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(54,54) = D7.read().range(54,54);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[55] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".55", sc_time_stamp().to_string().c_str(), D7.read().range(55,55).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(55,55) = D7.read().range(55,55);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[56] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".56", sc_time_stamp().to_string().c_str(), D7.read().range(56,56).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(56,56) = D7.read().range(56,56);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[57] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".57", sc_time_stamp().to_string().c_str(), D7.read().range(57,57).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(57,57) = D7.read().range(57,57);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[58] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".58", sc_time_stamp().to_string().c_str(), D7.read().range(58,58).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(58,58) = D7.read().range(58,58);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[59] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".59", sc_time_stamp().to_string().c_str(), D7.read().range(59,59).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(59,59) = D7.read().range(59,59);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[60] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".60", sc_time_stamp().to_string().c_str(), D7.read().range(60,60).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(60,60) = D7.read().range(60,60);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[61] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".61", sc_time_stamp().to_string().c_str(), D7.read().range(61,61).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(61,61) = D7.read().range(61,61);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[62] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".62", sc_time_stamp().to_string().c_str(), D7.read().range(62,62).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(62,62) = D7.read().range(62,62);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
           if ( WEM7.read()[63] == 1 ) {

#if defined(__GNUC__) && BDW_USE_SCV
            m_p8_tx_0 = m_p8_tx_stream->begin_write_tx( A7.read(), D7.read() );
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s%s: %s: write 0x%s to 0x%x\n", name(), ".63", sc_time_stamp().to_string().c_str(), D7.read().range(63,63).to_string(SC_HEX).c_str(), (int)A7.read().to_uint());
#endif
        mem[A7.read().to_uint()].range(63,63) = D7.read().range(63,63);
#if defined(__GNUC__)
            sync_put(A7.read().to_uint());
#endif
           }
        }

#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread8() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p9_tx_0.is_active() ) {
        m_p9_tx_stream->end_tx( m_p9_tx_0, Q8.read() );
    }
#endif
    if (CE8 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A8.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A8.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A8.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A8.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p9_tx_0 = m_p9_tx_stream->begin_read_tx( A8.read() );
#endif
            sync_get(A8.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A8.read().to_uint()].to_string(SC_HEX).c_str(), (int)A8.read().to_uint());
#endif
        Q8 = mem[A8.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread9() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p10_tx_0.is_active() ) {
        m_p10_tx_stream->end_tx( m_p10_tx_0, Q9.read() );
    }
#endif
    if (CE9 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A9.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A9.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A9.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A9.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p10_tx_0 = m_p10_tx_stream->begin_read_tx( A9.read() );
#endif
            sync_get(A9.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A9.read().to_uint()].to_string(SC_HEX).c_str(), (int)A9.read().to_uint());
#endif
        Q9 = mem[A9.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread10() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p11_tx_0.is_active() ) {
        m_p11_tx_stream->end_tx( m_p11_tx_0, Q10.read() );
    }
#endif
    if (CE10 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A10.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A10.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A10.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A10.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p11_tx_0 = m_p11_tx_stream->begin_read_tx( A10.read() );
#endif
            sync_get(A10.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A10.read().to_uint()].to_string(SC_HEX).c_str(), (int)A10.read().to_uint());
#endif
        Q10 = mem[A10.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread11() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p12_tx_0.is_active() ) {
        m_p12_tx_stream->end_tx( m_p12_tx_0, Q11.read() );
    }
#endif
    if (CE11 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A11.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A11.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A11.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A11.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p12_tx_0 = m_p12_tx_stream->begin_read_tx( A11.read() );
#endif
            sync_get(A11.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A11.read().to_uint()].to_string(SC_HEX).c_str(), (int)A11.read().to_uint());
#endif
        Q11 = mem[A11.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread12() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p13_tx_0.is_active() ) {
        m_p13_tx_stream->end_tx( m_p13_tx_0, Q12.read() );
    }
#endif
    if (CE12 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A12.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A12.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A12.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A12.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p13_tx_0 = m_p13_tx_stream->begin_read_tx( A12.read() );
#endif
            sync_get(A12.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A12.read().to_uint()].to_string(SC_HEX).c_str(), (int)A12.read().to_uint());
#endif
        Q12 = mem[A12.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread13() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p14_tx_0.is_active() ) {
        m_p14_tx_stream->end_tx( m_p14_tx_0, Q13.read() );
    }
#endif
    if (CE13 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A13.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A13.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A13.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A13.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p14_tx_0 = m_p14_tx_stream->begin_read_tx( A13.read() );
#endif
            sync_get(A13.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A13.read().to_uint()].to_string(SC_HEX).c_str(), (int)A13.read().to_uint());
#endif
        Q13 = mem[A13.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread14() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p15_tx_0.is_active() ) {
        m_p15_tx_stream->end_tx( m_p15_tx_0, Q14.read() );
    }
#endif
    if (CE14 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A14.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A14.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A14.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A14.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p15_tx_0 = m_p15_tx_stream->begin_read_tx( A14.read() );
#endif
            sync_get(A14.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A14.read().to_uint()].to_string(SC_HEX).c_str(), (int)A14.read().to_uint());
#endif
        Q14 = mem[A14.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
   void dummy_plm::thread15() {
#if defined(__GNUC__) && BDW_USE_SCV
    if ( m_p16_tx_0.is_active() ) {
        m_p16_tx_stream->end_tx( m_p16_tx_0, Q15.read() );
    }
#endif
    if (CE15 == 1) {
#if defined(__GNUC__)
      // Perform an address range check to prevent crashes on out of bounds
      // accesses in SystemC behavioral simulations.
      if((int)(A15.read()) <= 511) {
#endif

#if defined(__GNUC__) && defined(BDW_DEBUG_MEMS)
          if (   (sc_time_stamp().to_double() == m_last_write_time) 
		      && (sc_time_stamp().to_double() > 0.0) 
			  && (m_last_write_addr == A15.read().to_uint64())) {
            fprintf(stderr,"WARNING: %s: %s: Both a read and a write were performed at address %d\n", name(), sc_time_stamp().to_string().c_str(), (int)A15.read());
          }
          m_last_read_time = sc_time_stamp().to_double();
          m_last_read_addr = A15.read().to_uint64();
          m_num_errors++;
#if defined(__GNUC__) && (BDW_DEBUG_MEMS > 1)
          if (m_num_errors > BDW_DEBUG_MEMS) {
            fprintf(stderr,"Stopping simulation because more than %d errors have occurred\n", BDW_DEBUG_MEMS );
		    sc_stop();
          }
#endif
#endif

#if defined(__GNUC__) 
#if BDW_USE_SCV
            m_p16_tx_0 = m_p16_tx_stream->begin_read_tx( A15.read() );
#endif
            sync_get(A15.read().to_uint());
#endif

#if defined(__GNUC__) && defined(BDW_TRACE_MEMS)
        fprintf(stderr,"TRACE: %s: %s: read 0x%s from 0x%x\n", name(), sc_time_stamp().to_string().c_str(), mem[A15.read().to_uint()].to_string(SC_HEX).c_str(), (int)A15.read().to_uint());
#endif
        Q15 = mem[A15.read().to_uint()];
#if defined(__GNUC__)
      }
#endif
    }
  }
#if defined(STRATUS_VLG)
   dummy_plm::dummy_plm(const sc_module_name name)
      : sc_module( name ) ,CLK("CLK"),WE0("WE0"),CE0("CE0"),D0("D0"),A0("A0"),WEM0("WEM0"),WE1("WE1"),CE1("CE1"),D1("D1"),A1("A1"),WEM1("WEM1"),WE2("WE2"),CE2("CE2"),D2("D2"),A2("A2"),WEM2("WEM2"),WE3("WE3"),CE3("CE3"),D3("D3"),A3("A3"),WEM3("WEM3"),WE4("WE4"),CE4("CE4"),D4("D4"),A4("A4"),WEM4("WEM4"),WE5("WE5"),CE5("CE5"),D5("D5"),A5("A5"),WEM5("WEM5"),WE6("WE6"),CE6("CE6"),D6("D6"),A6("A6"),WEM6("WEM6"),WE7("WE7"),CE7("CE7"),D7("D7"),A7("A7"),WEM7("WEM7"),CE8("CE8"),Q8("Q8"),A8("A8"),CE9("CE9"),Q9("Q9"),A9("A9"),CE10("CE10"),Q10("Q10"),A10("A10"),CE11("CE11"),Q11("Q11"),A11("A11"),CE12("CE12"),Q12("Q12"),A12("A12"),CE13("CE13"),Q13("Q13"),A13("A13"),CE14("CE14"),Q14("Q14"),A14("A14"),CE15("CE15"),Q15("Q15"),A15("A15"), mem()
#else
   dummy_plm::dummy_plm( sc_module_name name)
      : sc_module( name ) ,CLK("CLK"),WE0("WE0"),CE0("CE0"),D0("D0"),A0("A0"),WEM0("WEM0"),WE1("WE1"),CE1("CE1"),D1("D1"),A1("A1"),WEM1("WEM1"),WE2("WE2"),CE2("CE2"),D2("D2"),A2("A2"),WEM2("WEM2"),WE3("WE3"),CE3("CE3"),D3("D3"),A3("A3"),WEM3("WEM3"),WE4("WE4"),CE4("CE4"),D4("D4"),A4("A4"),WEM4("WEM4"),WE5("WE5"),CE5("CE5"),D5("D5"),A5("A5"),WEM5("WEM5"),WE6("WE6"),CE6("CE6"),D6("D6"),A6("A6"),WEM6("WEM6"),WE7("WE7"),CE7("CE7"),D7("D7"),A7("A7"),WEM7("WEM7"),CE8("CE8"),Q8("Q8"),A8("A8"),CE9("CE9"),Q9("Q9"),A9("A9"),CE10("CE10"),Q10("Q10"),A10("A10"),CE11("CE11"),Q11("Q11"),A11("A11"),CE12("CE12"),Q12("Q12"),A12("A12"),CE13("CE13"),Q13("Q13"),A13("A13"),CE14("CE14"),Q14("Q14"),A14("A14"),CE15("CE15"),Q15("Q15"),A15("A15"), mem() 
#if defined(__GNUC__) 
#if defined(BDW_DEBUG_MEMS)
		, m_last_read_time(-1), m_last_write_time(-1), m_last_read_addr(0), m_last_write_addr(0), m_num_errors(0)
#endif
		, m_iface(0)
#endif
#endif
   {
#ifdef STRATUS_HLS
      SC_CTHREAD( thread0, (CLK).pos() );
#else
      SC_METHOD( thread0 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread1, (CLK).pos() );
#else
      SC_METHOD( thread1 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread2, (CLK).pos() );
#else
      SC_METHOD( thread2 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread3, (CLK).pos() );
#else
      SC_METHOD( thread3 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread4, (CLK).pos() );
#else
      SC_METHOD( thread4 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread5, (CLK).pos() );
#else
      SC_METHOD( thread5 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread6, (CLK).pos() );
#else
      SC_METHOD( thread6 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread7, (CLK).pos() );
#else
      SC_METHOD( thread7 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread8, (CLK).pos() );
#else
      SC_METHOD( thread8 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread9, (CLK).pos() );
#else
      SC_METHOD( thread9 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread10, (CLK).pos() );
#else
      SC_METHOD( thread10 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread11, (CLK).pos() );
#else
      SC_METHOD( thread11 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread12, (CLK).pos() );
#else
      SC_METHOD( thread12 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread13, (CLK).pos() );
#else
      SC_METHOD( thread13 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread14, (CLK).pos() );
#else
      SC_METHOD( thread14 );
      sensitive << (CLK).pos();

#endif
#ifdef STRATUS_HLS
      SC_CTHREAD( thread15, (CLK).pos() );
#else
      SC_METHOD( thread15 );
      sensitive << (CLK).pos();

#endif
      dont_initialize();
 
   }

const unsigned long dummy_plm::implemented = 1;
const unsigned long dummy_plm::pipelined = 0;
const float dummy_plm::area = 32.0;
const float dummy_plm::delay = 0.2;
const float dummy_plm::setup_time = 0.06;
const unsigned long dummy_plm::latency_WE0 = 1;
const unsigned long dummy_plm::latency_WEM0 = 1;
const unsigned long dummy_plm::latency_CE0 = 1;
const unsigned long dummy_plm::latency_D0 = 1;
const unsigned long dummy_plm::latency_A0 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM0 = 1;
const unsigned long dummy_plm::latency_WE1 = 1;
const unsigned long dummy_plm::latency_WEM1 = 1;
const unsigned long dummy_plm::latency_CE1 = 1;
const unsigned long dummy_plm::latency_D1 = 1;
const unsigned long dummy_plm::latency_A1 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM1 = 1;
const unsigned long dummy_plm::latency_WE2 = 1;
const unsigned long dummy_plm::latency_WEM2 = 1;
const unsigned long dummy_plm::latency_CE2 = 1;
const unsigned long dummy_plm::latency_D2 = 1;
const unsigned long dummy_plm::latency_A2 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM2 = 1;
const unsigned long dummy_plm::latency_WE3 = 1;
const unsigned long dummy_plm::latency_WEM3 = 1;
const unsigned long dummy_plm::latency_CE3 = 1;
const unsigned long dummy_plm::latency_D3 = 1;
const unsigned long dummy_plm::latency_A3 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM3 = 1;
const unsigned long dummy_plm::latency_WE4 = 1;
const unsigned long dummy_plm::latency_WEM4 = 1;
const unsigned long dummy_plm::latency_CE4 = 1;
const unsigned long dummy_plm::latency_D4 = 1;
const unsigned long dummy_plm::latency_A4 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM4 = 1;
const unsigned long dummy_plm::latency_WE5 = 1;
const unsigned long dummy_plm::latency_WEM5 = 1;
const unsigned long dummy_plm::latency_CE5 = 1;
const unsigned long dummy_plm::latency_D5 = 1;
const unsigned long dummy_plm::latency_A5 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM5 = 1;
const unsigned long dummy_plm::latency_WE6 = 1;
const unsigned long dummy_plm::latency_WEM6 = 1;
const unsigned long dummy_plm::latency_CE6 = 1;
const unsigned long dummy_plm::latency_D6 = 1;
const unsigned long dummy_plm::latency_A6 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM6 = 1;
const unsigned long dummy_plm::latency_WE7 = 1;
const unsigned long dummy_plm::latency_WEM7 = 1;
const unsigned long dummy_plm::latency_CE7 = 1;
const unsigned long dummy_plm::latency_D7 = 1;
const unsigned long dummy_plm::latency_A7 = 1;
const unsigned long dummy_plm::bits_per_maskbit_WEM7 = 1;
const unsigned long dummy_plm::latency_CE8 = 1;
const unsigned long dummy_plm::latency_A8 = 1;
const unsigned long dummy_plm::latency_CE9 = 1;
const unsigned long dummy_plm::latency_A9 = 1;
const unsigned long dummy_plm::latency_CE10 = 1;
const unsigned long dummy_plm::latency_A10 = 1;
const unsigned long dummy_plm::latency_CE11 = 1;
const unsigned long dummy_plm::latency_A11 = 1;
const unsigned long dummy_plm::latency_CE12 = 1;
const unsigned long dummy_plm::latency_A12 = 1;
const unsigned long dummy_plm::latency_CE13 = 1;
const unsigned long dummy_plm::latency_A13 = 1;
const unsigned long dummy_plm::latency_CE14 = 1;
const unsigned long dummy_plm::latency_A14 = 1;
const unsigned long dummy_plm::latency_CE15 = 1;
const unsigned long dummy_plm::latency_A15 = 1;
const unsigned long dummy_plm::reg_mem_inputs = 0;
const unsigned long dummy_plm::reg_mem_outputs = 0;
const unsigned long dummy_plm::regs_at_mem_inputs = 0;
const unsigned long dummy_plm::regs_at_mem_outputs = 0;
const unsigned long dummy_plm::regs_at_mod_inputs = 0;
const unsigned long dummy_plm::regs_at_mod_outputs = 0;
const unsigned long dummy_plm::no_spec_reads = 1;
const unsigned long dummy_plm::is_mem = 1;
const unsigned long dummy_plm::clock_multiplier = 0;
const bool dummy_plm::has_extra_ports = false;
const bool dummy_plm::is_prototype = false;
const char* dummy_plm::naming_convention = 0;
 
#endif // dummy_plm_CC

